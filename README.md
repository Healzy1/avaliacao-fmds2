# 🎬 CineShare - Catálogo de Filmes Colaborativo

Este projeto é uma aplicação web completa desenvolvida para a **Segunda Avaliação** da disciplina de **Frameworks Modernos para Desenvolvimento de Sistemas**.

O sistema consiste em um catálogo de filmes onde os usuários podem cadastrar, visualizar, editar e excluir títulos (CRUD), visualizar métricas em um dashboard e autenticar-se via conta Google.

---

## 👥 Integrantes do Grupo
* **Nome Completo:** Ícaro Gabriel Alves dos Reis
    * **Matrícula:** 20230004966
* **Nome Completo:** Guilherme Henrique Anschau Xavier De Oliveira
    * **Matrícula:** 20230021439

---

## 🚀 Funcionalidades Principais

* **🔐 Autenticação e Segurança:**
  * Login seguro via Google (OAuth) gerenciado pelo Supabase.
  * Proteção de rotas (Guards): Apenas usuários autenticados acessam o sistema.
  * Persistência de sessão (o usuário não precisa logar toda vez que atualiza a página).

* **📊 Dashboard Analítico:**
  * Métricas em tempo real: Total de filmes, média geral de notas e gênero favorito.
  * Gráfico de distribuição de notas (Excelentes, Bons, Regulares, etc.).
  * Destaques automáticos: Filmes com maior e menor nota.

* **🛠️ CRUD Completo de Filmes:**
  * **Create (Criação):** Cadastro de filmes com título, gênero, ano, nota (0-10), status e URL da capa.
  * **Read (Leitura):** Visualização flexível com alternância entre **Modo Grade** (Cards estilo Netflix) e **Modo Lista** (Tabela detalhada). Inclui busca por título e ordenação dinâmica.
  * **Update (Atualização):** Edição completa dos dados do filme via formulário modal.
  * **Delete (Exclusão):** Remoção de registros com janela de confirmação para evitar acidentes.

* **🎨 Interface (UI/UX):**
  * Layout responsivo com Vuetify (Menu lateral "Rail" e Header fixo).
  * Feedback visual (Snackbars) para ações de sucesso ou erro.
  * Tema Escuro/Claro (Dark Mode).

---

## 🛠️ Tecnologias Utilizadas

**Frontend:**
* Vue.js 3
* Vuetify (Interface e Layout)
* Pinia (Gerenciamento de Estado e Sessão)
* Vue Router (Navegação e Proteção de Rotas)

**Backend:**
* Express.js (API REST)
* Supabase (Banco de Dados PostgreSQL e Auth)

---

## 📂 Estrutura do Projeto

O projeto está organizado como um *monorepo*, separando claramente as responsabilidades de Backend (API) e Frontend (Interface).

```text
avaliacao-fmds2/
├── backend/                 # API REST (Express + Supabase)
│   ├── controllers/         # Lógica de Negócio (O "Cérebro" da API)
│   │   └── FilmeController.js
│   ├── routes/              # Definição das Rotas (Endpoints)
│   │   └── filmes.js
│   ├── utils/               # Configurações e Conexões
│   │   └── supabase.js      # Cliente do Supabase
│   ├── index.js             # Ponto de entrada (Entry Point)
│   └── package.json         # Dependências do Backend
│
└── frontend/                # Aplicação Web (Vue 3 + Vuetify)
    ├── index.html           # Ponto de entrada
    ├── vite.config.mjs      # Configuração do Vite
    └── src/
        ├── main.js          # Inicialização do Vue
        ├── App.vue          # Componente Raiz
        ├── supabase.js      # Cliente de Auth
        │
        ├── layouts/         # Layouts (Menu Lateral e Barra Superior)
        │   └── default.vue  
        │
        ├── pages/           # Telas do Sistema (Rotas)
        │   ├── index.vue    # Dashboard
        │   ├── login.vue    # Tela de Login
        │   └── filmes.vue   # CRUD de Filmes
        │
        ├── components/      # Componentes Reutilizáveis
        │   ├── AppHeader.vue
        │   ├── MenuList.vue
        │   ├── FilmeCard.vue
        │   ├── FilmeFormDialog.vue
        │   └── ConfirmDialog.vue
        │
        ├── stores/          # Gerenciamento de Estado (Pinia)
        │   ├── auth.js      # Sessão do Usuário
        │   └── filmes.js    # Lógica do CRUD
            └── index.js     # (O arquivo de configuração do Pinia)
        │
        └── router/          # Configuração de Rotas e Guards
            └── index.js
```

---

## 📦 Instruções de Instalação e Execução

Pré-requisito: Ter o **Bun** instalado.

### 1. Configuração do Banco de Dados (Supabase)

1. Crie um projeto no [Supabase](https://supabase.com).
2. Vá até o **SQL Editor** e execute o script abaixo para criar a tabela:

```sql
CREATE TABLE filmes (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  titulo TEXT NOT NULL,
  genero TEXT,
  ano INTEGER,
  nota INTEGER, -- De 0 a 10
  status TEXT, -- Ex: 'assistido', 'quero ver'
  capa_url TEXT
);
```
⚠️ Importante: Desative o RLS (Row Level Security) da tabela filmes ou crie policies adequadas.

### 2. Rodando o Backend (Terminal 1)
O backend é a API que conecta o sistema ao banco de dados.

1. Abra um terminal na pasta backend:

```bash
cd backend
```

Crie um arquivo chamado .env dentro da pasta backend com o seguinte conteúdo:

```env
PORT=3000
SUPABASE_URL=sua_url_do_supabase_aqui
SUPABASE_KEY=sua_chave_anon_publica_aqui
```

Instale as dependências e inicie o servidor:

```bash
bun install           # Baixa as dependências (Express, Supabase, etc)
bun --watch index.js  # Liga o servidor na porta 3000
```

Deve aparecer a mensagem: 🚀 Servidor rodando com Bun em http://localhost:3000

### 3. Rodando o Frontend (Terminal 2)
O frontend é a interface visual (Site). Mantenha o terminal do backend rodando e abra um novo.

Abra um terminal na pasta frontend:

```bash
cd frontend
```

Crie um arquivo chamado .env dentro da pasta frontend com o seguinte conteúdo:

```env
VITE_SUPABASE_URL=sua_url_do_supabase_aqui
VITE_SUPABASE_KEY=sua_chave_anon_publica_aqui
```

(Use as mesmas chaves do Supabase que configurou no backend).

Instale as dependências e inicie o site:

```bash
bun install      # Baixa as dependências (Vue, Vuetify, Pinia, etc)
bun run dev      # Liga o site (porta 3001)
```

Acesse o link exibido no terminal (O frontend rodará em http://localhost:3001) para usar o sistema.

### 🔒 Acesso ao Sistema
Para acessar as funcionalidades restritas (CRUD e Dashboard), clique no botão "Entrar com Google" na tela de login.

---
## 📄 Licença
Projeto desenvolvido exclusivamente para fins acadêmicos na disciplina de Frameworks Modernos para Desenvolvimento de Sistemas.